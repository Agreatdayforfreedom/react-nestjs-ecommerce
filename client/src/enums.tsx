export enum Order {
  priceASC = 'price%ASC',
  priceDESC = 'price%DESC',
  stock = 'stock',
  news = 'news',
}

export enum Enum_PaymentType {
  LIBSCREDITS = 'LIBSCREDITS',
  //paypal, strapi, etc
}
